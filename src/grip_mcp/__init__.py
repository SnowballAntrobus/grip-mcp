"""grip-mcp: a deterministic fretboard engine as an MCP server.

Milestone 0 status: this package currently ships the frozen quality table
(``data/qualities.toml``) and the versioned descriptions module. The V1
server (DESIGN.md §6) lands after the table freeze.
"""

__version__ = "0.1.0.dev0"
